# Servidor
PORT=4000
NODE_ENV=development

# Base de datos MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=escuela_musica

# Autenticación
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio
JWT_EXPIRES_IN=8h

# URL pública del frontend (se usa para construir el enlace del QR de asistencia)
FRONTEND_URL=http://localhost:5173

# Token rotativo de asistencia (QR)
ASISTENCIA_TOKEN_SECRET=cambia_esto_por_otro_secreto_largo_y_aleatorio
ASISTENCIA_TOKEN_INTERVALO_MS=180000
